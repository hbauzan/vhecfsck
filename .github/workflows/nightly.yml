# =============================================================================
# DISABLED — do not uncomment to "fix CI".
# -----------------------------------------------------------------------------
# This file used to be the Ubuntu nightly matrix. It was turned off on
# 2026-08-30 when the repo was private and runners were billed.
#
# The repo is public now. `ci.yml` already runs `make verify` on
# ubuntu-latest × Python 3.11/3.12/3.13 on push. P9-10 (local Docker
# `make verify`) is **cancelled** and is not a reason to restore this file.
#
# `make verify-full` and engine-SDK drift stay local / on demand. Restoring a
# scheduled nightly is a new ticket, not P9-10. Do not restore the cron from
# memory.
#
# The original workflow is preserved as comments (copy-paste). The stub below
# (`workflow_dispatch` + `if: false`) exists only because GitHub flags a
# workflows/*.yml file with no `on:` as invalid. It never runs on schedule.
# =============================================================================
#
# name: nightly
#
# on:
#   schedule:
#     - cron: "0 5 * * *"
#   workflow_dispatch:
#
# permissions:
#   contents: read
#
# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true
#
# jobs:
#   verify-full:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v7
#       - uses: astral-sh/setup-uv@v10.0.1
#         with:
#           enable-cache: true
#       - run: uv python install 3.12
#       - run: uv sync --group dev
#       - run: make verify-full
#
#   engine-sdk-drift:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v7
#       - uses: astral-sh/setup-uv@v10.0.1
#         with:
#           enable-cache: true
#       - run: uv python install 3.12
#       - run: uv sync --group dev
#       - name: Upgrade optional engine SDKs when declared
#         # Extras are empty until adapter tickets land; keep the job for drift visibility.
#         run: |
#           uv pip list
#           echo "Engine extras still empty (P5/P7); drift job is a placeholder until SDKs are declared."
#       - run: make verify

name: nightly

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Actions disabled permanently; local gate is make verify (P9-10 TBD)."
